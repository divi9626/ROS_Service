#pragma once

#include <string>

/**
 * Structure to store the default display message 
 * Type of the message is string
 */
struct DefaultString {
  std::string default_msg = "Robots are awesome";
};